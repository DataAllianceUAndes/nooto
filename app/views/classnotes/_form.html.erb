<%= form_with(model: classnote) do |form| %>
  <% if classnote.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(classnote.errors.count, "error") %> prohibited this classnote from being saved:</h2>

      <ul>
        <% classnote.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :course_id, style: "display: block" %>
    <%= form.select :course_id, options_for_select(Course.all.map{|i|[i.title, i.id]}) %>
  </div>

  <div>
    <%= form.label :files %>
    <%= form.file_field :files, :multiple => true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
