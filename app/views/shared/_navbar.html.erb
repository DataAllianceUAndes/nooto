<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to "Nooto", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="collapse">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Courses", courses_path, class: "nav-link #{is_active(path: courses_path)}" %>
        </li>
        <li class="nav-item">
          <%= link_to "Classnotes", classnotes_path, class: "nav-link #{is_active(path: classnotes_path)}" %>
        </li>
        <% if current_user != nil %>
          <li class="nav-item">
            <%= link_to "Profile", user_path(current_user), class: "nav-link #{is_active(path: user_path(current_user))}" %>
          </li>
        <% end %>
      </ul>
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item" style="padding-right: 10px">
          <%= button_to "Login", new_user_session_path, class: "nav-link btn btn-outline-success" %>
        </li>
        <li class="nav-item">
          <%= button_to "Register", new_user_registration_path, class: "nav-link btn btn-outline-info" %>
        </li>
      </ul>
    </div>
  </div>
</nav>
